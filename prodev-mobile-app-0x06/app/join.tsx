import React from "react";
import { StyleSheet, Text, View } from "react-native";

const join = () => {
  return (
    <View>
      <Text>join</Text>
    </View>
  );
};

export default join;

const styles = StyleSheet.create({});
